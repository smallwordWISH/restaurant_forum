  <% if restaurant.is_liked?(current_user) %>
    <%= link_to '',unlike_restaurant_path(restaurant), method: :post, class: "thumb-show glyphicon glyphicon-thumbs-up" %>
    <em class="like-count-show"><%= restaurant.liked_users.uniq.count %></em>
  <% else %>
    <%= link_to '',like_restaurant_path(restaurant), method: :post, class: "thumb-transt glyphicon glyphicon-thumbs-up" %>
    <em class="like-count-transt"><%= restaurant.liked_users.uniq.count %></em>
  <% end %>